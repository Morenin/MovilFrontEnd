import { EmailValidator } from "@angular/forms";

export interface User{
    actived: number,
    cicle_id:number,
    cicle_name:String,
    created_at:Date,
    email:String,
    id:number,
    name:String,
    num_offer_applied:number,
    surname:String,
    type:String,
}